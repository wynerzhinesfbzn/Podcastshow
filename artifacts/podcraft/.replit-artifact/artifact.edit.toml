kind = "api"
previewPath = "/podcraft"
title = "PodCraft"
version = "1.0.0"

[[services]]
localPort = 5000
name = "PodCraft Flask"
paths = ["/podcraft"]

[services.development]
run = "cd artifacts/podcraft && PORT=5000 BASE_PATH=/podcraft python app.py"
